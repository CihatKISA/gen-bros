# ============================================
# AI Content Topic Generator - Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!

# ============================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================
# PostgreSQL connection string for Neon or any PostgreSQL database
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?sslmode=require
# Get this from: https://console.neon.tech (for Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# ============================================
# PAYLOAD CMS CONFIGURATION (REQUIRED)
# ============================================
# Secret key for JWT token signing and encryption
# MUST be at least 32 characters long
# Generate with: openssl rand -base64 32
PAYLOAD_SECRET=your-secret-key-min-32-characters-long-here-change-this-in-production

# Public URL where your application is hosted
# Development: http://localhost:3000
# Production: https://your-domain.vercel.app
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# ============================================
# OPENAI API CONFIGURATION (REQUIRED)
# ============================================
# OpenAI API key for ChatGPT integration
# Get this from: https://platform.openai.com/api-keys
# Must start with 'sk-'
OPENAI_API_KEY=sk-your-openai-api-key-here

# ============================================
# REDIS CONFIGURATION (OPTIONAL)
# ============================================
# Upstash Redis for rate limiting and caching
# If not provided, in-memory fallback will be used
# Get this from: https://console.upstash.com
# Note: Use UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN for REST API
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-rest-token-here

# ============================================
# APPLICATION CONFIGURATION (REQUIRED)
# ============================================
# Public URL for the application (used in client-side code)
# Must match PAYLOAD_PUBLIC_SERVER_URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment
# Options: development | production | test
NODE_ENV=development

# ============================================
# EMAIL CONFIGURATION (OPTIONAL)
# ============================================
# SMTP settings for Payload CMS email features
# Only required if you want to enable email verification
# or password reset functionality
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password

# ============================================
# DEPLOYMENT NOTES
# ============================================
# For Vercel deployment:
# 1. Set all REQUIRED variables in Vercel dashboard
# 2. Ensure DATABASE_URL points to your Neon PostgreSQL instance
# 3. Generate a new PAYLOAD_SECRET for production
# 4. Update PAYLOAD_PUBLIC_SERVER_URL and NEXT_PUBLIC_APP_URL to your Vercel domain
# 5. Add OPENAI_API_KEY from your OpenAI account
# 6. Optionally add Upstash Redis credentials for better rate limiting
#
# For local development:
# 1. Copy this file to .env
# 2. Fill in your actual values
# 3. Never commit .env to git (it's in .gitignore)
#
# Environment validation:
# - All REQUIRED variables are validated on application startup
# - Missing or invalid variables will prevent the app from starting
# - Check src/lib/validation/schemas.ts for validation rules
